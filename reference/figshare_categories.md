# Select figshare categories and return corresponding integer identifier.

These identifiers should then be added in deposit metadata as, for
example, `subject(categories=c(1,2))`.

## Usage

``` r
figshare_categories()
```

## See also

Other meta:
[`dcmi_terms()`](https://docs.ropensci.org/deposits/reference/dcmi_terms.md),
[`deposits_metadata_template()`](https://docs.ropensci.org/deposits/reference/deposits_metadata_template.md)
